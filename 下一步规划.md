# 下一步规划

修复已完成并通过测试。下面是建议的后续路线，按「先稳、再强、再好用」排。

---

## 阶段 A — 稳定可用（1–2 天）

目标：从「能跑」到「日常敢用」。

| # | 事项 | 说明 |
|---|---|---|
| A1 | 真实链接回归 | 用抖音/B站/YouTube 各 1–2 条真实链接走完：解析 → 下载 → 历史落库 |
| A2 | 任务卡片显示标题 | `TaskProgress` 增加 `title`/`platform`，前端不再只显示 task_id |
| A3 | 下载失败可重试 | 任务列表加「重试」按钮，复用同一 URL 重新入队 |
| A4 | Cookie 过期提示 | sidecar 解析 401/登录失效时返回明确错误码，前端引导去设置页 |
| A5 | git init + 首次提交 | 已有 `.gitignore`；建议立即建仓，避免再丢/误传敏感文件 |

**验收**：端到端下载成功 ≥3 平台；失败任务能在 UI 看懂原因并一键重试。

---

## 阶段 B — 下载能力补齐（3–5 天）

| # | 事项 | 说明 |
|---|---|---|
| B1 | 画质/格式选择 | 解析结果 streams 暴露到 UI，默认最高画质，可选手动 |
| B2 | 字幕/封面下载 | yt-dlp 侧已能取；写入可选开关 |
| B3 | 断点续传 | 分块下载已有 Range；补 `.part` 恢复逻辑 |
| B4 | 限速与并发设置 | UI 暴露 `RateLimiter` 与任务并发数（现写死 5） |
| B5 | 图文作品下载 | sidecar 已识别 `image_post`，补多图打包 zip |
| B6 | 直播录制 | 检测 `live` 类型，改用 ffmpeg 拉流落盘 |

**验收**：可选 1080P/4K；大文件中断后可续传；设置页可调限速。

---

## 阶段 C — 产品化（约 1 周）

| # | 事项 | 说明 |
|---|---|---|
| C1 | 桌面壳 | `tauri` 或 `cargo-packager` 打包，隐藏浏览器窗口；sidecar/ffmpeg 一并捆绑 |
| C2 | 旧 GUI 归档 | `bilibili-downloader/` 仅保留 venv 与依赖清单，其余移 `legacy/` 或独立仓库 |
| C3 | 命名统一 | 产品名与目录名对齐（例如统一为 MediaDL / BillBill），避免 douyin/BillBill 混用 |
| C4 | Release 构建 | `cargo build --release` + 前端 build 一键脚本；CI 可选 GitHub Actions |
| C5 | 设置持久化 | 代理、并发、输出目录写入 SQLite `settings` 表（表已存在） |
| C6 | 错误 UX | `alert` 换成 Toast；WS 断线有横幅提示 |

**验收**：双击 exe 可用；设置重启后仍在；无浏览器依赖。

---

## 阶段 D — 加分项（按需）

- 播放列表 / 多 P 勾选下载
- 下载完成后系统通知（已有旧版 `notifications.py` 可参考）
- 历史记录打开文件所在文件夹
- 批量导入 txt/csv 链接
- 单元测试覆盖 sidecar 协议与路径校验（路径校验已有；补协议表驱动测试）

---

## 建议本周执行顺序

```
A5 git init
  → A1 真实链接回归（暴露剩余 bug）
  → A2/A3 任务标题 + 重试（体验立刻变好）
  → B1 画质选择（最常用缺口）
  → A4 Cookie 过期提示
```

### 风险与依赖

- **抖音接口/反爬**：依赖 f2 版本；升级 f2 前先锁 `requirements` 版本。
- **ffmpeg 捆绑**：打包阶段必须带上，否则 DASH 合并在干净机器上必挂。
- **端口**：默认已改为 `18080`；若用户环境仍冲突，用 `DDL_PORT` 覆盖。
- **Cookie 安全**：继续禁止入库；若做桌面壳，考虑系统凭据管理器而非明文配置。

---

## 当前基线（2026-09-13）

- `cargo test -p core-engine`：3 passed（含真实 sidecar ping）
- `cargo test -p server`：1 passed（项目根解析）
- `/api/health`：`sidecar=true, ffmpeg=true`
- 路径逃逸 `output_dir=../evil`：已拒绝
- 默认监听：`http://127.0.0.1:18080`
