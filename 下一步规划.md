# 下一步规划

多轮迭代后，核心产品能力已齐。下表为完成状态与可选增强。

---

## 阶段 A — 稳定可用

| # | 事项 | 状态 |
|---|---|---|
| A1 | 真实链接回归 | 待你用真实链接验证 |
| A2 | 任务卡片显示标题 | ✅ |
| A3 | 下载失败可重试 | ✅ |
| A4 | Cookie 过期提示 | ✅ |
| A5 | git init + 首次提交 | ✅ `771028c` / 迭代 `c4eb832` |

---

## 阶段 B — 下载能力

| # | 事项 | 状态 |
|---|---|---|
| B1 | 画质/格式选择 | ✅ |
| B2 | 字幕/封面下载 | ✅ 设置页开关 |
| B3 | 断点续传 | ✅ `.part` 恢复 |
| B4 | 限速与并发设置 | ✅ 设置页 + SQLite 持久化 |
| B5 | 图文作品下载 | ✅ 多图落盘 |
| B6 | 直播录制 | 可选增强（未做） |

---

## 阶段 C — 产品化

| # | 事项 | 状态 |
|---|---|---|
| C1 | 桌面壳 (Tauri) | 可选增强 |
| C2 | 旧 GUI 归档 | ✅ 独立 git + ignore |
| C3 | 命名统一 | 部分（BillBill DL） |
| C4 | Release 构建 | ✅ `build_release.bat` |
| C5 | 设置持久化 | ✅ |
| C6 | 错误 UX | ✅ Toast + WS 断线横幅 |

---

## 当前产品能力清单

- 单链接解析下载（抖音 f2 + yt-dlp 多平台）
- 抖音主页/合集批量勾选下载
- 画质/格式选择，DASH 自动合并
- 图文多图、封面、字幕旁路下载
- 断点续传、限速、并发可配并持久化
- 任务队列：进度、取消、重试、标题/平台展示
- SQLite 历史 + FTS 全文检索
- Cookie 诊断与失效引导
- 健康检查 API、本机 127.0.0.1 绑定、路径逃逸防护

## 建议你本地验收（A1）

1. `.\启动网页版.bat`
2. 抖音视频 / B站视频 / YouTube 各贴一条链接
3. 中断一次大文件下载再点重试，确认续传
4. 设置页改限速/并发，重启后仍在

## 可选后续

- 直播录制 (B6)
- Tauri 桌面壳 (C1)
- 任务队列拖拽排序 / 批量导入 txt
- 历史记录「打开所在文件夹」

---

## 阶段 C — 产品化（约 1 周）

| # | 事项 | 说明 |
|---|---|---|
| C1 | 桌面壳 | `tauri` 或 `cargo-packager` 打包，隐藏浏览器窗口；sidecar/ffmpeg 一并捆绑 |
| C2 | 旧 GUI 归档 | `bilibili-downloader/` 仅保留 venv 与依赖清单，其余移 `legacy/` 或独立仓库 |
| C3 | 命名统一 | 产品名与目录名对齐（例如统一为 MediaDL / BillBill），避免 douyin/BillBill 混用 |
| C4 | Release 构建 | `cargo build --release` + 前端 build 一键脚本；CI 可选 GitHub Actions |
| C5 | 设置持久化 | 代理、并发、输出目录写入 SQLite `settings` 表（表已存在） |
| C6 | 错误 UX | `alert` 换成 Toast；WS 断线有横幅提示 |

**验收**：双击 exe 可用；设置重启后仍在；无浏览器依赖。

---

## 阶段 D — 加分项（按需）

- 播放列表 / 多 P 勾选下载
- 下载完成后系统通知（已有旧版 `notifications.py` 可参考）
- 历史记录打开文件所在文件夹
- 批量导入 txt/csv 链接
- 单元测试覆盖 sidecar 协议与路径校验（路径校验已有；补协议表驱动测试）

---

## 建议本周执行顺序

```
A5 git init
  → A1 真实链接回归（暴露剩余 bug）
  → A2/A3 任务标题 + 重试（体验立刻变好）
  → B1 画质选择（最常用缺口）
  → A4 Cookie 过期提示
```

### 风险与依赖

- **抖音接口/反爬**：依赖 f2 版本；升级 f2 前先锁 `requirements` 版本。
- **ffmpeg 捆绑**：打包阶段必须带上，否则 DASH 合并在干净机器上必挂。
- **端口**：默认已改为 `18080`；若用户环境仍冲突，用 `DDL_PORT` 覆盖。
- **Cookie 安全**：继续禁止入库；若做桌面壳，考虑系统凭据管理器而非明文配置。

---

## 当前基线（2026-09-13）

- `cargo test -p core-engine`：3 passed（含真实 sidecar ping）
- `cargo test -p server`：1 passed（项目根解析）
- `/api/health`：`sidecar=true, ffmpeg=true`
- 路径逃逸 `output_dir=../evil`：已拒绝
- 默认监听：`http://127.0.0.1:18080`
